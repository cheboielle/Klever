export function normalizeHex(value:string):string|null{const s=value.trim().replace(/^#/,'');if(/^[a-f\d]{3}$/i.test(s))return '#'+s.split('').map(c=>c+c).join('').toUpperCase();return /^[a-f\d]{6}$/i.test(s)?'#'+s.toUpperCase():null;}
export function hsvToHex(h:number,s:number,v:number){const c=v*s,x=c*(1-Math.abs((h/60)%2-1)),m=v-c;const rgb=h<60?[c,x,0]:h<120?[x,c,0]:h<180?[0,c,x]:h<240?[0,x,c]:h<300?[x,0,c]:[c,0,x];return '#'+rgb.map(n=>Math.round((n+m)*255).toString(16).padStart(2,'0')).join('').toUpperCase();}
export function hexToHsv(hex:string){const [r,g,b]=hex.slice(1).match(/../g)!.map(v=>parseInt(v,16)/255),max=Math.max(r,g,b),min=Math.min(r,g,b),d=max-min;return {h:d===0?0:((max===r?(g-b)/d:max===g?(b-r)/d+2:(r-g)/d+4)*60+360)%360,s:max===0?0:d/max,v:max};}
function rgb(hex:string){return hex.slice(1).match(/../g)!.map(v=>parseInt(v,16));}
function mix(a:string,b:string,t:number){return '#'+rgb(a).map((v,i)=>Math.round(v*(1-t)+rgb(b)[i]*t).toString(16).padStart(2,'0')).join('').toUpperCase();}
export function contrast(a:string,b:string){const lum=(h:string)=>rgb(h).map(v=>v/255).map(v=>v<=.04045?v/12.92:((v+.055)/1.055)**2.4).reduce((sum,v,i)=>sum+v*[.2126,.7152,.0722][i],0);const x=lum(a),y=lum(b);return (Math.max(x,y)+.05)/(Math.min(x,y)+.05);}
export function customTheme(hex:string){const brand=normalizeHex(hex)??'#226A50';let accent=brand;const soft=mix(brand,'#FFFFFF',.91),paper=mix(brand,'#FFFFFF',.975);for(let i=0;i<30&&contrast(accent,soft)<4.5;i++)accent=mix(accent,'#000000',.1);return {label:'Custom',brand,accent,ink:mix(brand,'#000000',.78),soft,paper,line:mix(brand,'#FFFFFF',.8),onBrand:contrast(brand,'#FFFFFF')>=4.5?'#FFFFFF':'#000000'};}
