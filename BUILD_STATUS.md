# Klever Assets build status

Updated: 9 September 2026.

## Current stage

Stage A is in progress. A1 database/access foundation is deployed to the supplied development Supabase project; A2 asset/team/readings and service and recurring task screens are implemented and usable in the local preview. Stage A is not complete. No signed native build, store submission, production release, live notifications, billing, or legacy cutover has occurred.

## Projects and access

- GitHub origin: https://github.com/cheboielle/Klever.git. The complete source is committed on main using the explicit builder identity Codex <codex@localhost> and pushed to origin/main. GitHub browser authentication through Git Credential Manager succeeded; credentials remain managed by Git Credential Manager, not source files.
- Supabase: https://blzubtujrxpcnfphcrny.supabase.co. CLI login/link established. Publishable key in ignored apps/mobile/.env.local. Administration keys used only in process memory.
- Migrations 202609090001 foundation, 202609090002 meter units, 202609090003 editable details, 202609090004 service schedules, 202609090005 service evidence, 202609090006 private photo downloads, 202609090007 service corrections, 202609090008 tasks, 202609090009 profile photos, 202609090010 starter library, 202609090011 issues/compliance, 202609090012 delivery, 202609090013 reminder rules, and 202609090014 exports are applied. The service-photo, notification-worker (disabled) and export-report Edge Functions are deployed. Hosted signup disabled, development redirect configured, upload limit 10 MiB. Other hosted settings preserved.
- Owner che@klever.co.nz provisioned with 90 days of explicit internal development access. Owner confirmed successful login. Private setup helper has stopped. Six-character minimum requested by owner and recorded in Supabase config.
- Expo project a33d4b36-8e52-4f3f-9de9-b960fac88b3b configured with slug klever-nz. Signing, package/bundle IDs and EAS ownership not yet verified. Owner has both phone platforms.
- Local preview: http://localhost:8081/ while the development server runs. Browser preview sessions are memory-only; reloads may require login. Native credentials use SecureStore.

## Authoritative instructions

Read AGENTS.md, Klever_Assets_Build_Spec_v4_1.md and Klever_Assets_Implementation_Plan_v4_1.md. Historical v4 documents are superseded. JSON/PDF schedules remain unverified starter guidance. Existing assets.klever.co.nz is a potential migration source only; do not copy its design.

## Implemented

- Expo SDK 57 app: login, live access checks, optional device lock, asset register/search, asset/type creation, status/assignment controls, online meter readings and history, staff deactivation/remote sign-out.
- Admin asset editing: name, serial, type; Hours/Kilometres correction with explicit current reading, reason and confirmation. Corrections preserve prior reading units, append audit details and advance the meter revision. Stale old-unit readings cannot overwrite the corrected meter. Cross-unit correction targets are rejected.
- Admin team editing: name and phone number for existing members, with role/access/credentials unchanged. Add-team-member remains planned; owner explicitly said not to reprioritize invitations.
- Editable asset photos and team profile pictures support camera/gallery, preview, upload retry, replacement and removal. Asset register thumbnails use the private current image. Only admins edit these photos; readers retain parent asset/profile permissions. A new upload is finalized before replacing the current picture; obsolete pictures are no longer downloadable through the client. Service/task evidence is unchanged.
- Protected PostgreSQL commands/RLS for tenants, role/assignment limits, owner/seat invariants, live Auth sessions, immutable readings and private default-deny media bucket.
- Service scheduling supports per-asset definitions, type defaults, asset overrides, separate baselines, Hours/Km/Calendar/Both server-calculated due states, editing and per-asset archival.
- Online service completion requires live-camera capture, an embedded capture timestamp and successful binary JPEG upload. Completion is idempotent and advances baselines once. Delayed submissions preserve evidence with pending_correction state. Private admin cost/notes are separate; history/photo views enforce actor/assignment permissions.
- Admin service correction/void controls require a reason and explicit replacement baseline, append correction records, preserve original evidence, and safely retry without duplicates.
- Recurring asset/business tasks: daily/weekly/fortnightly/monthly/custom calendar schedules, stable checklist items, editable configuration, archive/restore, required notes/photos, camera/gallery attachment, private history and admin void corrections that retain original evidence.
- Business tasks let admins choose one completion for everyone or individual completion by each active technician. Separate technician occurrences advance independently; admin progress shows who is still due. Newly enrolled/reactivated technicians are included, inactive staff excluded from outstanding work. Mode changes preserve history and invalidate pending work from the previous mode.
- Task finalization requires uploaded JPEG evidence when configured, serializes competing submissions, and advances a late task to its next future scheduled date. Monthly recurrence preserves its intended day through short months. Readings, issues, service and task submissions now use the durable local queue described below.

- Truckmount starter library preserves all supplied model/family mappings, service intervals and recommendation notes. Admins preview and acknowledge starter guidance before per-asset application. Atomic retries do not duplicate setup or overwrite edits. Baselines remain unknown until entered. Engine-less families exclude engine-only daily checks; hour templates cannot be applied to kilometre assets. The obsolete Phase 2 odometer sentence is omitted from displayed vehicle guidance, with the original source JSON preserved.
- Assigned technicians/admins report issues with immutable description, urgency, performer/asset snapshots and both timestamps. Admins append resolution notes. Urgent reports enqueue one push and email entry per active admin/owner in a durable outbox; the sender is deployed but disabled pending provider setup and authorization.
- Compliance dates support labels, renewal, reminder lead days (30/7 defaults), assigned-recipient option, archive/restore, version checks and asset audit history. Reminder delivery remains in A3.

## Latest verification

- 136 local tests passed using actual migrations and RLS in PGlite with synthetic managed Auth/Storage fixtures. TypeScript passed.
- 27 hosted checks passed using temporary users in two businesses, including edits, meter corrections, assignment/tenant isolation, required JPEG upload, private downloads, service/task corrections with safe retries, shared task deduplication, separate technician progress, asset/profile photo upload/replacement/removal, template deduplication, urgent-issue queuing/resolution, compliance renewal, session revocation and deactivation. Synthetic photos, records, queued notifications and all four Auth test accounts were removed successfully after this run.
- iOS, Android and web JavaScript bundles exported successfully with the library, issue and compliance screens alongside all earlier workflows. Expo dependency compatibility also passed. These are not signed binaries or device proofs.
- Signed-in browser inspected: asset edit form prepopulates fields; changing unit reveals current reading/reason/confirmation; team edit shows name/phone. Forms closed without saving changes to user records.
- Actual Supabase refresh-token cascade to sessions verified. Hosted media upload/download/revocation checks now pass. Hosted read-only transitions and actual camera/push/lock behaviour on phones still need coverage.

## Next work

Continue with A2 usability/device checks and A3 offline device proof, notification scheduling/provider activation and exports; A4 import/media, guidance checks, backup/restore and Klever dogfooding. Stage B portal, invitations, billing and release follow the core proof gate. No legacy export has been supplied. Staff onboarding remains in the established plan.

Submission IDs, command payloads and queued photos persist across app restarts. Native airplane-mode/restart proof is still outstanding. Camera code compiles but real-device capture/watermark/permissions are not yet verified. Native push/lock proof, backup/media restore, exports and migration are not complete. README.md documents setup/testing commands. Do not claim Stage A acceptance yet.

## Owner-directed refinements

Hours/Kilometres selection and later correction explicitly override the original odometer exclusion. Future service baselines/countdowns/exports must use the selected unit; changing unit requires explicit baseline re-entry, never automatic conversion of hour templates or past evidence. Numeric columns/RPCs with historical hours names are retained for compatibility and qualified by meter_unit.

PGlite substitutes for unavailable Docker for local database tests; hosted HTTP tests remain a separate gate. No other product-scope changes.


Photo download implementation note: hosted testing exposed CDN reuse of an authenticated Storage GET after session revocation. Direct client reads are now disabled by migration 006. The service-photo POST endpoint verifies the caller's JWT via PostgREST and performs a fresh live-session/assignment/evidence check before streaming bytes with no-store headers. Gateway verify_jwt=false is deliberate: authentication/authorization occurs through that database request, not by trusting a client body or unverified claims. Never replace this endpoint with signed URLs or direct cached Storage downloads. New uploads specify zero cache age.

Service setup forms were inspected in the signed-in browser without saving user data. No camera was activated by the builder; hosted evidence was a generated synthetic JPEG. Native camera verification remains required.

Owner addition: editable team profile pictures are required. Asset photos were already scoped. Both camera/gallery flows are now implemented in the planned setup/media work. They are replaceable private configuration images, not immutable service evidence.

Task milestone verification: actual hosted photo upload/download and next-request revocation passed for task evidence as well as service evidence. The browser preview was inspected at its sign-in screen; the new signed-in task forms have not yet been visually inspected. Native camera/gallery behaviour still needs actual phone testing. No user records were changed by these tests.

Owner addition (9 September 2026): company-wide tasks must offer shared or per-technician completion (example: each technician washes their own van). This explicitly supersedes the original shared-only restriction and is implemented in migration 008 and TaskPanel. Tasks and instruction snapshots retain their completion mode.

Profile-photo milestone: migration 009 and updated photo endpoint deployed to the development project. All 86 local tests, TypeScript and all three platform exports passed; 24 hosted checks passed. Browser preview was at the sign-in screen, so signed-in visual inspection of these new controls is still pending. Actual phone camera/gallery capture is unverified. Superseded configuration photo bytes remain private and require the planned unreferenced-media cleanup/retention job; removing a picture withdraws client access immediately.

Library/issues/compliance milestone: migrations 010–011 deployed to the development Supabase project; 100 local tests and TypeScript passed, 24 hosted checks passed, and iOS/Android/web JS exports passed. Signed-in visual checks remain pending because the browser preview was at login. No manufacturer verification was performed or claimed. Urgent notifications are queued only; scheduled compliance reminders and actual push/email delivery are not implemented. Synthetic outbox entries were deleted in the verified fixture cleanup.

Offline milestone in progress: AsyncStorage 2.2.0 stores account-scoped snapshots and serialized commands; native photo copies use app document storage and web preview photo blobs use IndexedDB. Read/service/task/issue forms queue before acknowledging save. Foreground polling, explicit retry and reconnect refresh revalidate access; server denial never falls back to cached reads. The 24-hour entry window is capped by the known access end, while already queued work survives expiry/read-only. Confirmed access revocation or sign-out clears local cache, queue and media. Pending readings support ordered later readings/services; a blocked reading does not stop independent issues. Pending controls allow explicit confirmation/review of readings and confirmed removal of blocked local copies.

116 tests now pass, including actual sync-coordinator tests with stubbed transports for upload failure, lost acknowledgements, required photo ordering, role/cache isolation and revocation. TypeScript and all three platform exports passed after integration. Real iOS/Android airplane-mode restart, background execution and native secure-storage refresh behaviour remain unverified. Expired credentials are read from existing SecureStore only after a network refresh failure to identify the cached account; they do not extend server auth or the verified offline window. Service/task photo views currently need a connection after upload; locally queued photos are retained until server acknowledgement. No backend migration or hosted fixture run was needed for this client-only queue change.


Notification delivery foundation (9 September 2026): migration 202609090012 and the notification-worker function are deployed to the development project. Device registrations are private, session-bound and removed by remote sign-out. Service-only expiring leases prevent simultaneous claims; stale worker acknowledgements are rejected. Delivery records retain retry/receipt state, handle invalid phone tokens and cancel queued operational messages for inactive recipients or read-only tenants. Expo ticket acceptance is followed by receipt checks; Resend retries reuse the job ID and stop before its deduplication window expires. Worker configuration is disabled by default; no provider credentials or activation secret were configured and no real messages were sent. The native enable-alerts control registers a phone after explicit permission. Recurring rules, server scheduling, automatic invocation, token refresh integration and real-phone delivery remain outstanding.

Final verification for this session: 126 local tests passed (including database isolation and simulated provider failures), TypeScript passed, and web/iOS/Android JavaScript exports passed. Five hosted schema/privilege assertions passed for the new migration; the deployed worker's unauthenticated request returned 401. The earlier 24 hosted workflow checks remain the latest full synthetic workflow run; no new business fixtures were created this session. This is development deployment only, with no signed native binary or Stage A acceptance.

Next action: implement the scoped recurring notification rules/settings and server scheduler, then CSV/maintenance-PDF exports. Provider setup and actual phone checks can be collected together when needed. Continue autonomously through ordinary development work; owner explicitly requested no step-by-step permission checks.


Reminder/export milestone (9 September 2026): migrations 013–014 are deployed. Admin reminder settings expose recipients, channels, local time, weekdays and business timezone. Server scans cover missing daily meter logs, task due/overdue with seven-day admin escalation, independent technician task occurrences, service due transitions/reminders and compliance lead dates; issue and assignment changes enqueue transactionally. Urgent minimum channels remain unchanged. Delivery rechecks current asset assignments. Local-date occurrence keys suppress retries/repeated daylight-saving hours; skipped times run at the next valid minute. The hosted klever-reminder-scan cron job exists and is INACTIVE. Delivery activation, worker invocation and actual phone notification tests remain pending provider configuration. No real notifications sent.

Admin CSV/PDF exports now cover asset registers, readings, service history and completed task history, with per-asset or business scope. Export access is server-enforced and remains available while read-only. Maintenance PDFs include original unit/readings, saved names/instructions, both timestamps, correction records and actual embedded photos. Missing recorded photo paths are identified; failed downloads abort the report rather than silently omit expected evidence. Noto Sans (SIL OFL) preserves Māori names; CSV quoting protects spreadsheet-formula input. Native exports use the share/save sheet with temporary file cleanup; web preview downloads files. export-report Edge Function is deployed; all reads/photos retain the caller's live authorization and a final access recheck.

Verification: 136 local tests passed and TypeScript passed. iOS/Android/web JS exports passed. A synthetic 13-page maintenance PDF was rendered and visually inspected; text extraction verified Unicode/corrections and 11 embedded images. The hosted PDF also rendered successfully with real uploaded synthetic JPEG evidence. 27 hosted workflow checks passed, including PDF, export isolation and read-only exports. All five synthetic photos, fixture records/outbox entries and four Auth accounts were cleaned up successfully. No real user records were edited by verification. New signed-in UI and native share-sheet behaviour still require device testing.

Remaining limits: export generation is synchronous and aborts above 64 MiB of fetched photo bytes; large real datasets need measured hosting tests and background processing if required. No large-report acceptance claim. Reporting-currency setup, source-control backup, native background execution/token refresh integration, real-device offline/camera/lock/push/sharing proof, provider activation, legacy import and combined database/media backup/restore remain to complete Stage A. Core web portal/billing still follow Stage A proof.


Source-control checkpoint: initial implementation committed locally. GitHub remote is reachable for reads but push returned “could not read Username” with interactive prompts disabled. A GitHub sign-in for Git is needed before upload; no credentials were requested in chat or stored in source. Next independent work is native background/token lifecycle integration and preparing the combined device/provider setup checks. Existing real-device, Resend/sender and legacy-data inputs remain pending. No further tests were run after documentation-only status changes.


GitHub connection resolved: owner completed the secure Git Credential Manager browser sign-in. Initial implementation and status commits were successfully uploaded to https://github.com/cheboielle/Klever.git on main. Source backup is now on GitHub; database/photo recovery backups remain separate outstanding work. No application code changed in this authentication step.


Shutdown recovery check (2026-09-09 21:46 local): Git working tree was clean at c5d6f09; source, dependencies and ignored connection configuration were present. Restarted Expo web preview on http://localhost:8081/. Metro rejected its old temporary cache and successfully rebuilt it automatically. Both HTML and application JavaScript returned HTTP 200. All 136 tests and TypeScript passed again. No application code or hosted data was changed. Preview runs in the current development-server process and stops if this computer shuts down. Resume native background/token lifecycle work and device/provider setup from the latest milestone; reminder scan and delivery remain inactive.


Overnight checkpoint (9 September, 22:05 local): owner authorized independent overnight development. A task continuation named klever-overnight-build is scheduled through 8am Pacific/Auckland on 10 September, every 15 minutes when available. Keep this computer/app awake. Resume useful work from this status; do not repeat completed slices.

Native background sync is implemented using pinned expo-background-task/expo-task-manager 57.0.16. The top-level OS callback loads the signed-in account's durable queue, preserves data on refresh/network errors, rechecks account identity and flushes at most three pending commands per run. The existing live server checks, retry IDs, photo ordering and conflict handling remain in force. Login configures the job, logout unregisters it, and foreground syncing remains the primary path. Generated iOS config has processing/fetch background modes and the expected Expo task identifier. No OS scheduling or airplane-mode proof is claimed yet.

Phone alert registration refreshes on foreground and native token changes after existing OS permission; only the explicit enable button requests permission. Removing OS permission withdraws the current user's device token using migration 015, deployed to development. The command remains allowed read-only and cannot delete another user's registration. Real sending stays disabled. Also repaired garbled punctuation in App.tsx.

Validation: 142 tests passed, including bounded queue batches, failed auth refresh preservation, account changes and private unregister access. TypeScript passed and final web/iOS/Android bundles exported successfully. Two hosted unregister-role assertions passed. No test users or real-business mutations were needed for this slice.

Expo CLI account check: temporary EAS CLI 23.2.0 required a flat dependency layout. Working command is pnpm --config.node-linker=hoisted --package=eas-cli@23.2.0 --package=ejs@3.1.10 dlx eas whoami. It reports Not logged in. Owner Expo sign-in is required before native builds. Do not waste another run on the already-resolved missing-ejs error. No build/signing/cost was initiated.

Next independent work: complete business app-lock/reporting-currency settings, then implement/verify database-plus-media recovery tooling and prepare native device acceptance checks. Docker/pg_dump were not available at inspection; consult official Supabase backup/restore guidance and keep actual photo bytes separate from database metadata. Legacy export/media and real-device/provider inputs are still external gates; proceed with other Stage A work while owner sleeps.


Business-settings milestone (9 September, 22:20 local): migration 016 and currency-aware export-report deployed to development. Admins can edit business name/timezone, phone app lock and default reporting currency with stale-edit protection. Native enabling checks that phone credentials exist. New service submissions capture the displayed currency into the durable queue; history and CSV/PDF retain that currency after default changes, without conversion. Existing pre-settings development costs use the previous NZD default. Read-only settings remain readable but cannot be edited.

Validation: all 144 tests and TypeScript passed; the database/isolation suite passed again (113 checks) after hosted-script changes. Web/iOS/Android exports passed with the native compiler allowed outside the sandbox. PDF page 1 rendered and visually checked with NZD amount; the 13-page synthetic report generated successfully. All 29 hosted checks passed. All five synthetic photos, test records/outbox and four Auth users were removed. No real business settings were changed. Native lock and signed-in settings visual acceptance still need actual device testing. Next: verified media backup/recovery tooling, then combined database/photo restore and owner/device gates already listed.


Media-backup milestone (9 September, 22:24 local): scripts/media-backup.mjs implements operator-only evidence-byte backup and independent checksum verification, with full paginated listing, original object names, new-directory-only writes and before/after inventory comparison. A download failure, damaged file or concurrent Storage change prevents a completed manifest. Six focused tests plus the full suite pass: 150 tests total. Development storage was copied read-only into ignored tmp/media-backup-20260909-2223: one photo, 17,706 bytes; subsequent independent verification passed. No source objects changed. scripts/RECOVERY.md records commands and the combined restore procedure/gates.

This is a local verified media copy only. Daily offsite scheduling, a database dump, an approved independent restore target and combined recovery proof remain outstanding. Do not represent GitHub or this local directory as database backup coverage. Next overnight action: investigate obtaining supported database dump tooling/access without exposing credentials or resetting existing passwords; continue independent Stage A readiness fixes if that requires owner setup. Expo sign-in, actual devices, verified sender/provider activation and legacy data remain owner gates. Overnight continuation is still active through 8am local.


Database-recovery milestone (9 September, 22:38 local): official portable EDB PostgreSQL 17.11 tools downloaded/extracted under ignored tmp/postgresql-tools/pgsql/bin, compatible with hosted PostgreSQL 17.6. No Windows database service installed. Existing CLI temporary connection settings are parsed into process environment only, not executed as shell text or printed. Read-only archive needed the CLI's postgres role; earlier failed archives have no success manifest and are not backups. No passwords reset or hosted records changed.

scripts/database-backup.py now creates/verifies a custom archive with explicit linked-project check and checksum. Verified copy: tmp/database-backup-20260909-2240 (459,501 bytes). Scope: public/private/Auth/Storage/application migration history, excluding managed auth.schema_migrations/storage.migrations. Cluster roles, extension setup, provider settings/secrets and photos remain separate. scripts/rehearse-local-restore.py successfully restored that archive into a new password-protected loopback-only PostgreSQL process and reconciled all Storage plus submitted/current evidence paths with tmp/media-backup-20260909-2223. Result in tmp/restore-rehearsal-script-20260909/result.json. Restored counts and hosted aggregate counts match: 1 tenant, 2 assets, 1 Auth user, 0 service completions, 0 task completions, 1 photo. The temporary local database was stopped. No grants/hosted Auth or Storage endpoint proof is claimed: local rehearsal intentionally restores schema/data without provider ownership/grants.

Validation: 4 Python operator tests passed (credential isolation, no shell execution, wrong-project rejection, archive corruption); 113 database/isolation tests passed after final scripts. Existing 150-test full JS suite remains latest full run; no client changes this checkpoint. RECOVERY.md now records reproducible commands and exact limitations. Daily offsite scheduling plus separate hosted Supabase recovery remain gates needing an authorized destination; real devices/Expo login, notification provider setup and legacy source data remain external inputs. Next independent Stage A work: native notification-tap handling and remaining app readiness checks, preserving lock/current assignment checks. Keep Stage B deferred until actual core proof.


Notification-tap milestone (9 September, 22:47 local): native cold-start/foreground notification responses now open the current asset or company tasks after fresh server access checks. A newly enabled app lock is handled before opening details. Missing/reassigned/archived assets show an unavailable message; network failures retain an explicit retry instead of using cached permissions. Consumed responses are cleared only if still current; arbitrary URLs are not opened. The worker adds event kind alongside asset ID and is deployed but remains disabled. Its unauthenticated endpoint returned HTTP 401; no real notifications sent.

Validation: 156 JS/database tests passed, TypeScript passed after a nullability fix, the 113-test isolation suite passed again, and web/iOS/Android bundles exported successfully. Six focused alert-routing cases plus simulated delivery metadata are covered. Native notification-response lifecycle, actual lock/phone taps and delivery remain device/provider acceptance gates. No business fixtures or real records changed. Next independent work: complete the inactive worker invocation setup and check ordinary small-team notification throughput before provider activation. No Stage B advancement yet.


Notification-operations milestone (9 September, 23:02 local): migration 017 and updated notification-worker deployed. Worker now claims current recipients one at a time, processes up to 20 jobs with a 45-second delivery budget, and retains per-job lease acknowledgements. Provider calls respect the remaining deadline. Urgent issues take priority; up to 50 cancelled candidates are skipped while filling a requested claim. Existing assignment/membership/read-only checks remain intact. No real messages sent.

The private Vault-backed dispatcher and klever-notification-delivery cron job are deployed via supabase/operations/notification-worker-schedule.sql. It reads environment-specific URL/secret from Vault and schedules once per minute, INACTIVE. Both reminder scan and delivery schedules verified inactive. Anonymous/app dispatch and app claims all denied; unauthenticated Edge Function request returned 401. No provider keys, worker secret or activation flag configured. supabase/operations/NOTIFICATIONS.md records setup, actual device/send acceptance, activation and pause instructions.

Validation: all 163 JS/database tests and TypeScript passed. New tests cover a 15-recipient batch, deadline/backlog bounds, interrupted/stale acknowledgements, urgent priority and cancellation skipping. Four hosted privilege/inactive assertions passed. No mobile code changed in this milestone, so the preceding successful three-platform bundles remain current. Next: inspect remaining Stage A construction against the brief without re-auditing settled choices; focus on any real configuration-editing or workflow gaps. Actual native builds/devices, verified sender/provider activation, legacy data and approved offsite/full hosted recovery destinations remain the external gates.


Reading-correction milestone (9 September, 23:20 local): found and completed the missing ordinary admin control for correcting accepted/current readings, including mistaken setup values. New migration 018 correct_asset_reading requires online writable admin access, a reason, confirmation and expected meter revision. It appends a linked correction to the current accepted reading, or explicitly links the original asset/unit setup event when no reading exists in the current unit. It never fabricates an earlier reading, rewrites originals or records a service. Stable retry IDs preserve a lost acknowledgement without applying the correction twice. The UI explains retained history, preserves attempted details for retry and labels correction rows. CSV/PDF exports include setup-origin links. Migration and export-report deployed to development.

Validation: 167 tests and TypeScript passed; 118-test isolation suite passed again after hosted script edits; all three platform bundles passed. Synthetic reading-correction PDF rendered and visually checked. 30 hosted workflow checks passed, including setup correction, export link, duplicate retry and technician denial. All five synthetic photos, fixture records/outbox and four Auth users removed; real owner assets unchanged. Native/signed-in visual acceptance remains pending.

Specific remaining construction gaps observed during this focused pass: owner promote/demote/transfer and staff reactivation are implemented/protected in the database but lack app controls; asset-type rename has a backend RPC but no normal UI; asset archiving has a stored flag but no completed operator flow. Complete these scoped controls next, preserving history, assignment permissions and read-only exceptions. Do not reopen the settled product/stack decisions or advance Stage B.


Team-controls milestone (9 September, 23:32 local): app now exposes staff reactivation, owner-only admin promotion/demotion and ownership transfer through existing protected manage_staff RPC. Current owner identity is read from the admin-visible tenant row and shown correctly on the team list. Owner deactivation controls are hidden; server ownership protection remains authoritative. Remote sign-out/deactivation remain available online while read-only. Inline confirmations replace the previous React Native Alert-based confirmation so browser preview and native share the same flow. Deactivation loads assignments and offers direct review before or after the decision; it does not force reassignment before access can be removed. No account invitation UI added (Stage B).

Validation: 169 tests passed, including new ordinary-admin role denial and seat-limited reactivation with retained assignments; TypeScript and web/iOS/Android bundles passed. Existing database/hosted role checks remain current; no migration/server change or real team mutation was needed. Both available browser previews lacked a usable signed-in session (one login screen, one blank), so signed-in browser/native visual interaction is still pending. No user credentials were requested or modified. Next construction: asset-type rename and asset archive/restore controls, preserving logs/history and preventing archived assets from generating active work.

Asset-lifecycle milestone (9 September, overnight): migration 019 deployed to development. Admins can archive/restore assets and rename asset types. Archived assets retain assignments, readings, service history, photos and exports for admins; technicians lose access until restored. Archiving cancels pending asset alerts and blocks new or prepared sign-offs from finalizing while archived. Restore preserves existing baselines and assignments. Type renames append audit events without changing service definitions. The app separates active and archived assets and disables operational entry for archived assets.

Validation: 173 JS/database tests passed; TypeScript and web/iOS/Android exports passed. Isolation suite rerun passed all 124 checks. All 31 hosted synthetic checks passed, including technician archive/photo denial, admin retained evidence/export access, restore and type rename. Cleanup independently verified zero test tenants, users and photo objects for this run. No real asset was archived or renamed. Signed-in visual/native acceptance remains pending.

Next: check retained history access for removed service schedules, then continue any remaining independent Stage A work. Real devices/Expo login, notification provider setup, legacy source data and an approved daily offsite/full hosted recovery destination remain external gates. Overnight continuation remains active; do not advance Stage B before core proof or repeat completed verification without a new reason.

Past-service history milestone (10 September, around midnight): migration 020 and app Past service records section retain discoverability of submitted services after schedule removal or asset-type changes. The list uses the latest permitted submission snapshot, excludes active schedules, and respects current membership/assignment plus own-evidence limits for technicians. Existing protected history/photo endpoints display records. Removed schedules are read-only in this section and are not reactivated or given fabricated current baselines. Cached list reads follow the existing account/offline policy.

Validation: all 174 JS/database tests passed (125 isolation checks), TypeScript passed, web/iOS/Android exports passed, and diff whitespace check passed. Added removal/history discovery, other-performer exclusion, cross-tenant denial and read-only access coverage. Migration 020 deployed; hosted privileges confirmed anonymous execution denied, signed-in execution granted, migration recorded. No real records mutated and no synthetic hosted fixtures created in this slice. Signed-in visual/device acceptance remains outstanding. Source checkpoint is committed/pushed with this record.

Next independent check: retained task history/config removal and any remaining ordinary Stage A workflow gaps. Do not invent features or repeat whole-product audits. If construction is exhausted, preserve the current checkpoint and wait for the already-recorded Expo/device, notification-provider, legacy and recovery-destination inputs; Stage B still waits for core proof. Overnight schedule remains active until 8am local.

Task-history checkpoint (10 September, 00:13 local): archived task history/restoration already existed for admins. Exposed the existing archive filter to technicians so they can reach their own retained completions. No new editing authority or evidence access was granted. Added technician archived-list/history and forbidden restore assertions to the existing archive test. All 174 tests passed; after simplifying test role setup, all 125 isolation checks passed again. TypeScript, web/iOS/Android exports and whitespace check passed. No server migration or hosted mutation needed. README now points to the completed inactive notification dispatcher and current recovery instructions rather than obsolete setup gaps.

The scoped archive/history construction pass is complete. Next progress depends primarily on actual acceptance inputs: secure Expo sign-in and both phones for native testing; verified Resend sender/provider configuration for authorized test delivery; legacy export and original media for migration reconciliation; an approved offsite backup and separate hosted recovery destination. Do not repeatedly check unchanged accounts or rerun successful tests merely to stay busy. Resume a concrete remaining Stage A item if new evidence/input permits it; otherwise keep overnight follow-ups quiet until morning. Stage B remains gated on core proof. All real user records remain preserved.

Morning handover (10 September, final scheduled check before 8am): working tree was clean at f17ac21. Overnight construction finished around 00:13; later follow-ups stayed quiet because the recorded external gates had not changed. No further code/tests/provider activity is claimed during that waiting period. Overnight continuation paused at this final scheduled check. Latest proof remains 174 full tests, 125 isolation checks, TypeScript and all three platform exports; development migrations through 020 are deployed. Source is on GitHub. Native phone acceptance and public release remain unverified.

Owner's next action: secure Expo sign-in, then install/test internal builds on both phones with assistance. After that, finish verified notification sender setup, obtain legacy export/media and choose the offsite backup/separate recovery target. Do not ask for passwords or secret keys in chat. Resume Stage A acceptance from these inputs; retain disabled live notifications/billing and defer Stage B until core proof.

Android build setup (10 September, morning): owner completed Expo browser authentication; CLI confirmed linked project @klever-nz/klever-nz. Android package nz.co.klever.assets configured. Preview APK profile pins Node 24.19.0/pnpm 11.19.0 and includes only the public development Supabase URL/publishable key, so the cloud bundle does not rely on ignored local environment files. No server secret is included. 125 isolation checks passed after config changes. Expo created the managed Android signing keystore and accepted the 1.9 MB source upload for the first internal build. Build completion/installation is not yet verified. No store submission or plan purchase performed.

EAS needs the existing repository trusted in its child process: GIT_CONFIG_COUNT=1, GIT_CONFIG_KEY_0=safe.directory, GIT_CONFIG_VALUE_0=C:/Users/chebo/OneDrive/Desktop/SAM App. Do not reinitialize Git when EAS reports an uninitialized repo due to this Windows ownership check. Temporary CLI build-script prompts for dtrace-provider/protobufjs were left unselected; CLI succeeded. Next: check cloud build outcome, fix observed errors, then provide the APK link and test Android login/camera/offline/restart. Push notifications still require Android Firebase credentials plus the recorded sender/provider setup.

First Android build: 6076a547-f69c-4749-99ec-66264f126ac4, verified IN_QUEUE at the latest check. Page: https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/6076a547-f69c-4749-99ec-66264f126ac4 . Version 0.1.0 (1), internal distribution. Upload included working-tree Android config; EAS records previous HEAD 20b95c6, and those config changes are now saved as f68177b. No APK artifact exists yet; do not claim installation ready until FINISHED/artifact verified.

First real Android feedback (10 September, late morning): owner installed build 6076a547-f69c-4749-99ec-66264f126ac4 and reached assets/team screens. Reported picker-triggered relock/photo loss, repeated unlock attempts, keyboard overlap, pull-refresh relocking, hard-to-navigate setup controls, non-clickable dashboard tiles, missing staff invitations and Firebase setup error. This confirms installation/use only, not full device acceptance.

Fix slice implemented: refresh defaults to access/data checks without relocking; initial/genuine background return and newly enabled app lock still lock. Scoped native interaction guard covers camera/gallery permission/pickers, authentication and export sharing, with single-flight manual unlock. Lock uses an overlay without unmounting the workspace/forms, preserving drafts; automatic authentication is not started while the lock modal mounts. Inactive-only focus/shade changes do not request another unlock. KeyboardAwareScrollView now covers main/forms/settings screens with pinned Expo-compatible keyboard/reanimated/worklets dependencies. Safe-area layout clears system navigation. Dedicated Settings tab houses alert/business/type/reminder controls and exports, bottom tabs have labelled icons, hero is compact, dashboard tiles filter assets, and asset type/status, task scope/cadence, currency and reminder choices use dropdowns. Native Firebase error now explains configuration is pending; no delivery is claimed.

Validation: 177 tests passed including 3 native lifecycle cases; TypeScript passed. Three platform exports passed before the final small authentication/share guard refinements; cloud Android rebuild will validate final native packaging. 125 isolation checks passed after dropdown updates. Signed-in browser visual review unavailable (preview shows login); no credentials altered. Owner said no/not sure about Firebase, so Firebase client configuration and FCM service credentials remain required. Team invitation flow remains unbuilt (previous Stage B scope); next after this fixes build is the scoped staff onboarding work needed for multi-user device acceptance, without advancing the whole portal/billing release. No real business records or notifications changed.

Android feedback update submitted: build dacec64d-f3e9-4eed-af8e-005de20e2aed, version 0.1.0 (2), source commit 66ffe6a. Latest observed status NEW with no artifact yet. Page https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/dacec64d-f3e9-4eed-af8e-005de20e2aed . Reuses the existing managed signing key, so install as an update rather than uninstalling (preserve local drafts). Next verify cloud outcome and retest photo picker/save, keyboard edits, refresh, genuine leave/unlock and navigation on the owner's Android. Firebase project creation guidance given; Analytics unnecessary. Staff invitations remain a separate unfinished flow, not a claimed fix in build 2.

Firebase connection (10 September afternoon): owner supplied project klever-assets-9aada. Registered Android application nz.co.klever.assets via owner's Firebase console; app ID 1:202778817568:android:11af1d111580dc48beb845. Verified downloaded google-services.json matches project/package and contains no private key; integrated its public client configuration with app.json. 125 isolation tests passed using direct Vitest runner. pnpm script wrapper attempted dependency repair and aborted without TTY; no module purge approved/performed. Native/cloud packaging is next.

Verified feedback build dacec64d-f3e9-4eed-af8e-005de20e2aed FINISHED with APK artifact. Firebase config requires a subsequent build. Sending credential not created: automatic approval review explicitly rejected opening Generate new private key because creating a sensitive service credential needs specific owner confirmation. Do not bypass; request authorization to create the Firebase service-account key and upload directly to Expo's secure Android FCM credentials. No key downloaded, exposed or committed; delivery remains disabled. Continue public client configuration/build independently.

Firebase-enabled Android build submitted: f83b1235-a615-4682-b142-c3839bab27a0, version 0.1.0 (3), commit 4887cf4, latest NEW/no artifact. Page https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/f83b1235-a615-4682-b142-c3839bab27a0 . Uses existing signing key and includes the phone feedback fixes. Await owner approval for creating/uploading the private FCM sending credential; do not retry the blocked action without it. No notification sending activated.

Firebase sending credential completed (10 September): owner explicitly approved creation and direct secure Expo upload. Generated the Firebase service-account key, verified its project/account without printing private material, and uploaded it using EAS credentials for Android FCM V1. Expo confirmed successful upload and assignment to nz.co.klever.assets. No Play Store submission credential assigned. Removed the exact temporary Downloads key file after verified upload. No private key entered source/chat/status or app bundle. Notification worker and scan remain disabled; actual phone registration/delivery tests still pending. Firebase-enabled build f83b1235-a615-4682-b142-c3839bab27a0 is the install target once FINISHED.

Usability update (10 September, afternoon): owner explicitly retired the extra app/device authentication layer. Removed foreground/launch/notification unlock prompts and its business setting/plugin; normal Supabase sign-in, SecureStore sessions, access rechecks, deactivation, revocation, assignment and entitlement enforcement remain. Phone screen lock now protects an already-signed-in app. Retained native picker refresh guard to preserve photo flows. Migration 021 keeps legacy app_lock false and ignores old clients' p_lock requests. Added protected staff contact_email/job_title fields and edit/display controls; contact email does not mutate Auth login. Old three-argument staff edits preserve new fields. All six editable service/task/compliance dates use shared calendar with month/year/day choice, optional unknown baseline clearing, and local date-only formatting; reminder times use 48 half-hour options preserving any existing off-grid value. Top-right menu contains Settings/Sign out, leaving Assets/Tasks/Team as primary tabs. Settings retains business/type/reminder/export controls. Full UX redesign deferred by owner until functional acceptance. Updated authoritative brief/plan. Staff invitations remain separately unbuilt.

Validation: initial full run passed 175 tests after deleting obsolete lock tests; new date/time tests subsequently passed with isolation (128 checks: 126 database + 2 date/time). Final migration adjustment reran all 126 isolation checks successfully. TypeScript, whitespace checks and web/iOS/Android exports passed. Restricted sandbox blocked Hermes executable; approved normal export succeeded. Hosted migration initially rolled back on a pending-trigger ALTER constraint; removed unnecessary constraint (writes already RPC-only) and deployed successfully. Hosted columns verified; further read-only deployed access/permission verification next. No real staff contact fields or operational data altered. Native interaction acceptance still requires owner retest; no Android installation/build completion claimed yet.

Usability Android update submitted: 59ce0345-3a46-4b8c-b59d-b83290dfccdc, version 0.1.0 (4), source 638f5cc. Latest NEW, no artifact yet. Page https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/59ce0345-3a46-4b8c-b59d-b83290dfccdc . Hosted read-only verification confirms migration 021 recorded, zero enabled app locks, anonymous new staff RPC execution denied and authenticated execution granted (internal role/tenant/write checks covered by isolation). Source pushed to origin/main. Next verify FINISHED, then owner installs over existing app and retests photo save/refresh/foreground, email/title persistence, dates/times and menu. Do not uninstall pending offline work. Native proof and notification delivery remain unverified; no live communications activated.

Systematic testing pass (10 September evening): owner asked for coverage so they do not have to discover every option alone. Verified Android build 4 (59ce0345-3a46-4b8c-b59d-b83290dfccdc) FINISHED. Fresh 177 automated checks passed across 13 files; 126 database isolation checks passed again after expanding hosted smoke coverage. All 32 hosted checks passed against actual development Auth/database/photo/export endpoints, now including staff contact email/title permissions and unchanged sign-in email, plus ignoring legacy lock enablement. Used two disposable tenants, four users and five JPEGs; no real operational records edited and no notifications sent. Cleanup photo deletion succeeded; initial multiline SQL argument did not remove tenants, so account deletion correctly failed on FK protection. Executing the exact cleanup using db query --linked --file succeeded. Final cleanup removed four users and independently verified zero test tenants/users/photos. Use --file for multiline cleanup SQL on Windows.

TESTING_STATUS.md now tracks workflow coverage, gaps and the short remaining phone checklist. Browser control opened fresh exported app at http://127.0.0.1:8083/ (Python static server session 12482, current compiled app). Sign-in form verified; requested owner sign-in privately through UI, still awaiting reply. Existing localhost8081 preview was blank. Do not claim authenticated UI walkthrough or actual phone capture/offline/delivery proof. Next: after sign-in, exercise clearly labelled temporary UI records, menus, calendars, forms and persistence; fix observed issues and update test record. Team onboarding and external release gates remain incomplete. Source-only hosted test changes need no Android rebuild.

Signed-in browser acceptance pass (10 September, ~22:00): owner logged into iab tab 7 at 127.0.0.1:8083. Exercised real UI menu/settings/time picker, staff detail display/edit opening, asset create/edit/reopen, kilometre entry/countdown/history, Both service baseline/calendar, month/year compliance date, monthly task/checklist/required notes/completion/next due, company mode/custom recurrence controls, status counts/filter, archive access and CSV export preparation. TESTING_STATUS.md records exact proof and untested gaps. Created only labelled QA asset serial QA-20260910-BROWSER and its dependent test records; archived it afterward, restoring three real active assets. Real assets/staff untouched; reminder selection was cancelled unsaved; no real communications sent. QA history retained in archive.

Found stale task completion feedback after successful sync. Task now refreshes history on sync and shows confirmed receipt only for its submission ID; service feedback likewise verifies exact history and distinguishes pending_correction. Added two receipt tests. All 179 automated tests (including 126 isolation checks), TypeScript and whitespace passed. All-platform bundle validation and next Android build follow. Current signed-in browser remains on prior build-4 export; no claim of post-fix signed-in/device proof. No migration needed. Next continue untested correction/technician/native workflows and scoped team onboarding; do not treat UI DOM checks as proof of native keyboard/camera/offline/push.

Sync-feedback export validation: web, Android and iOS bundles all passed. Submitting an internal Android update next; no release/store submission.

Android sync-feedback build submitted: 5321b229-5f94-4bae-9599-b8e53dad0b63, version 0.1.0 (5), source 229a07f. Latest NEW with no artifact. Page https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/5321b229-5f94-4bae-9599-b8e53dad0b63 . Verify FINISHED before installation; install over existing app. Post-fix UI/native confirmation remains pending. Owner's browser left signed in on active Assets with original three assets, QA test asset archived.

Overnight continuation reactivated at owner's request on 10 September: existing klever-overnight-build heartbeat resumes every 15 minutes through 8am Pacific/Auckland 11 September. Priorities: verify build 5, remaining independent UI/correction/restore checks, then scoped staff invitation/onboarding with synthetic tests and exact delivery blockers. Preserve data, run isolation/focused checks, commit/push milestones, keep release/provider limitations honest, and leave morning handover. No permission to email real staff or enable live billing/notification delivery inferred. Full UX redesign remains deferred.

Overnight acceptance checkpoint (10 September 22:36): verified Android build 5 FINISHED. Continued actual signed-in browser build-4 session: archived QA asset blocked operational saves, restore preserved 1,250 km and schedules/history; confirmed downward correction to 1,245 km showed -5 km linked to retained original; task void used calendar-selected 11 September, required confirmation, retained checklist/notes evidence with void reason, and refreshed next due correctly. Rearchived QA asset; real assets/staff/settings unchanged. No code changes or repetitive test run in this acceptance slice. TESTING_STATUS.md updated. Next heartbeat should start scoped staff invitation/onboarding implementation (not repeat completed build/restore/reading/task-void checks). Need secure expiring single-use acceptance, protected admin invite creation, seat enforcement at acceptance, profile details, and explicit provider-delivery handling; no real emails or live notification activation. Post-fix sync-message UI verification requires a newer signed-in preview/device and remains pending rather than reopening the owner session.

Staff invitation backend slice (10 September ~22:58): migration 022 deployed to development. Added admin-only invitation records/RLS and create_staff_invitation(p_id,p_email,p_name,p_phone,p_job_title), cancel_staff_invitation(p_id), my_staff_invitations(), accept_staff_invitation(p_id,p_name,p_phone,p_job_title). Invitation UUID is not a login credential: recipient must have a current Supabase Auth session and confirmed matching Auth email. Invites expire after seven days, cancellation/reuse are enforced, pending rows do not consume seats, and acceptance locks the same tenant row as provisioning/reactivation before rechecking current write access, seats, inviter admin status and existing membership. New members always start as technicians; owner promotion remains the existing separate flow. Cancelling pending access is allowed while read-only. Name/phone/title and contact email persist at acceptance. No plaintext passwords, tokens, emails or Auth user creation in the database slice.

Validation: 187 full tests passed, including 134 database isolation tests (8 new invitation tests); TypeScript and whitespace checks passed. 33 hosted checks passed, including actual confirmed-Auth invitation acceptance, wrong-account denial and single-use rejection. Hosted runner now deletes staff_invitations before memberships. Five synthetic users, two tenants and five photos created solely for tests; cleanup verification follows below. No real staff invited, activated or emailed. No mobile code changed, so no redundant bundle/build requested. UI and Auth/email link delivery remain unimplemented and are explicitly not claimed complete.

Next overnight slice: build native admin Team invitation creation/list/cancel UI plus secure password-setup/acceptance journey. Existing App refresh signs out not_provisioned accounts; it must route a valid signed-in invitation recipient into onboarding instead, without weakening revoked/inactive handling. Use current official Supabase Auth documentation for secure invitation/password setup and existing Resend stack. Provider sender is still unconfigured, so do not send real email or invent success; complete testable flow and return an explicit delivery-setup state. Invitation acceptance requires verified email even if invite ID is known; do not create a custom password/token system or expose service credentials. Retain public signup disabled and do not advance unrelated portal/billing. Build 5 and browser restore/correction checks are already verified; do not repeat them.

Invitation backend hosted cleanup verified: zero remaining synthetic tenants, users, photos or invitation rows for this run. All real records and archived QA evidence preserved.


Invitation UI/Auth checkpoint (10 September ~23:16): added admin Team invitation preparation/list/cancel screens and a recipient personal-details/password/join screen. Only not_provisioned access routes into onboarding; inactive and remotely signed-out users still lose access and sign out. Fixed the independent offline sync check so it clears unprovisioned business cache without signing an invitee out before joining. Foreground refresh preserves an in-progress join form. Passwords are set through Supabase Auth, held only in form memory and cleared after success; acceptance still uses migration 022 verified-email/session/seat checks. Lost acceptance acknowledgements recheck current access. A successful password update is retained during an acceptance retry (including same-password provider responses).

Added native cold/warm invitation-link listener with explicit Continue and existing-session protection. Proposed server-to-app contract is kleverassets://join?token_hash=<Supabase proof>&type=invite (or magiclink for a supported existing-account flow). Parser rejects other hosts/routes, fragments, duplicate or extra parameters, recovery types and invitation-ID substitutes. It calls Auth verifyOtp only after confirming no existing session; token/provider errors are not echoed. No custom credential system or automatic account replacement. Provider generation/delivery remains UNIMPLEMENTED: evaluate email-client deep-link support and a code-entry fallback during that next slice, rather than claiming this listener alone completes emailed onboarding. Existing signup remains disabled.

Validation: all 192 tests across 15 files passed, including 134 database isolation tests, 3 invitation-link checks and new unprovisioned/revoked offline-sync regressions. TypeScript, whitespace and web/Android/iOS exports passed. Export output tmp/invitation-ui-export; owner browser remains on its existing signed-in build-4 export. These new UI screens are not yet browser/device accepted or installed. No new migration, hosted mutation or email; no redundant hosted smoke run after UI-only changes. Build 5 remains the latest finished APK. Defer a new Android build until the remaining invitation delivery/code-entry slice is ready so it includes a useful whole flow.

Next overnight action: implement a protected server invitation delivery endpoint using existing Supabase Auth/Resend, server-derived recipient/tenant, expiring Auth links or OTP, explicit configured/unconfigured state and honest delivery/retry handling. The current Team screen explicitly says saved/no email sent; replace that hardcoded setup state only when the endpoint supplies real status. Never return bearer links to admin clients or log them. Verified sender and authorization for real recipients are still unavailable; use synthetic provider transport and actual Auth test accounts where useful, clean exact fixtures. Complete independent implementation before asking for sender input. Then verify onboarding UI with synthetic accounts where secure access permits and submit a meaningful internal Android update. Overnight heartbeat updated and verified ACTIVE through 8am 11 September; do not repeat completed build-5/restore/correction checks. Commit/push this tested checkpoint with the status record.


Invitation delivery milestone (10 September ~23:35): migration 023 and staff-invitation Edge Function deployed to development. Current-admin/tenant RPCs derive the recipient, reject expired/cancelled/accepted invites or changed inviter authority, enforce write access, claim one delivery attempt with a two-minute cooldown and recheck before provider send. Only service role records matching attempt outcomes; no Auth codes, hashes or links stored in invitation rows. Auth generate_link uses invite for new accounts and magiclink for existing unprovisioned accounts, privately; Resend receives a one-use email code. Actual hosted verification proved both new and already-existing unconfirmed accounts work with verifyOtp email type. Email-code entry avoids depending on email-client custom-link support. The previously added deep-link listener remains supplementary; delivery uses code entry.

Team now saves and requests email sending, shows actual not_configured/sent/failed/unknown/cooldown outcomes, and offers explicit resend. A sent receipt means provider acceptance, not inbox delivery. Uncertain network/provider/receipt outcomes are not automatically retried: admins check with the recipient before resending a fresh code. Native sign-in has “I have an invitation”; verified users confirm details, set their own password and accept as technicians. Email/password data remain form/Auth memory only. Actual delivery is explicitly OFF: INVITATIONS_ENABLED is unset and the deployed endpoint returned not_configured in both hosted cases. RESEND_API_KEY and verified NOTIFICATION_FROM plus recipient authorization are required before activation; no scheduled notifications or real mail enabled.

Validation: all 201 tests in 16 files passed, including 139 database isolation tests. Mobile TypeScript and standalone handler TypeScript passed; whitespace and web/Android/iOS exports passed (tmp/invitation-delivery-export). After correcting the hosted test cleanup script to include automatically created notification_rules, all 139 isolation checks passed again. Ten focused hosted checks passed using one disposable tenant and three Auth accounts: deployed-disabled result, real Auth generation for new/existing unconfirmed accounts, no duplicate immediate send, confirmed one-use code/session, password setup, technician acceptance and forbidden invitation sending. Every Resend call was intercepted; no invitation email delivered. Initial cleanup SQL rolled back on reminder-default FK, then corrected exact-ID transaction and account deletion succeeded; final query verified zero tenants/users/invitations for this run. No real records or archived QA evidence changed.

Browser interaction: separate hidden iab tab 8 at 127.0.0.1:8084 on the new export (Python session 51466) verified invitation form opens, required empty controls disable Verify, incomplete input gives a helpful validation error, and Back/reopen clears the code and email fields. Owner tab 7 at 8083 remains untouched. Authenticated admin/join screen interactions, actual native code entry/password setup and real email delivery remain unverified; the actual backend journey passed via HTTP. Do not present this as phone acceptance. README records secure activation and exact cleanup steps.

Next: commit/push this tested milestone and submit an internal Android update; record build reference and verify completion once. Then continue feasible acceptance (remaining service/photo/cost/correction/output coverage or safe synthetic-role UI testing) rather than repeating completed checks. Sender verification/authorized recipients, actual device capabilities, offsite recovery target and legacy data remain external gates. Full portal/Paddle and visual redesign remain deferred.


Android invitation update submitted: e55baf9f-ece1-4da2-9307-095121270a65, version 0.1.0 (6), source f41581e. Latest status NEW; completion/artifact not yet verified. Page https://expo.dev/accounts/klever-nz/projects/klever-nz/builds/e55baf9f-ece1-4da2-9307-095121270a65 . Existing managed signing key reused; install as an update over the existing app. No store submission or purchase. Next heartbeat should verify this build (do not submit a duplicate), then pursue remaining feasible acceptance. Sender/real-email and actual phone tests remain outstanding. Source f41581e pushed to origin/main; build reference recorded separately.


Isolated browser onboarding acceptance (10 September 23:42–11 September 00:00): added scripts/browser-invitation-smoke.mjs using pinned Playwright 1.63.0 in ignored tmp/browser-qa and installed Chrome with separate ephemeral contexts. Five actual UI checks passed: long-name header/menu at 412px width; admin invitation creation/cancellation with explicit no-email status; recipient code verification/details and six-character password setup; owner-visible accepted staff phone/title with pending invitation removed; admin deactivation and fresh technician-login denial. Codes/passwords remain memory-only; no real browser profile, email delivery, traces or operational records used. Initial test failed because it asserted row removal before the cancellation refresh finished; fixed the test wait, with no cancellation app change. Every run's exact disposable fixtures were cleaned before rerun. Final SQL verified zero TEST ONLY browser tenants and klever-ui test users. Existing owner tab 7 and archived QA records untouched.

Observed/fixed UI issue: a valid long business name pushed the top menu outside phone width. Header now truncates the business name and preserves menu/brand space. All 201 tests (139 isolation), TypeScript, all-platform bundles and whitespace passed after this app change. The final preview-helper change reran all 139 isolation checks successfully. No database migration needed. This small header fix is NOT in Android build 6; batch it into the next meaningful native update rather than submit another queued build solely for this change.

Preview diagnostics: icon boxes came from local asset serving, not a demonstrated Android icon failure. Combined-platform export omitted the nested web font copy; separate web-only export produced it. Standard Python http.server still returned 404 because the font's Windows path is 277 characters; an extended prefix alone broke root directory normalization. Added scripts/serve-web-preview.py to normalize paths before adding the Windows extended prefix. Verified root and font HTTP 200, FontFace ionicons loaded and actual rendered menu/tile/tab icons in tmp/font-check.png. Correct browser preview is now 127.0.0.1:8085 from tmp/acceptance-web-export, server session 82735. Earlier 8083/8084 owner/hidden previews are unchanged and use older combined exports; do not silently reload the owner's session. Headless test contexts are closed and their accounts deleted. README documents reproducible tooling and exact cleanup.

Android build 6 e55baf9f-ece1-4da2-9307-095121270a65 was checked once this slice and remained IN_QUEUE; no duplicate build or repeated polling. Next heartbeat may check its outcome, then pursue remaining isolated service/photo/cost/correction and actual export-output UI acceptance. Invitation/browser onboarding is now verified and should not be repeatedly retested without relevant changes. Actual Android camera/keyboard/offline/push and invitation code journey, verified sender/real email, recovery destination and legacy material remain external gates. Overnight continuation updated ACTIVE through 8am with these next steps. Save/push this browser/header/preview milestone before proceeding.


Service/photo/export browser acceptance (11 September 00:02–00:18): Android build 6 e55baf9f-ece1-4da2-9307-095121270a65 verified FINISHED; do not poll it again. Added scripts/browser-service-smoke.mjs with isolated owner/technician Chrome contexts and an explicitly simulated camera. Four complete UI checks passed: owner captures/stamps/uploads a service photo with NZD 125.50 and private notes, receives the exact confirmed-sync message and views evidence; technician submits/views own photo while admin evidence/cost/notes/correction controls stay hidden; owner void requires confirmation, retains original photo/cost and sets an explicit 1210-hour baseline (70 remaining, due 1310); actual CSV/PDF downloads include both performers, private details and correction history. Existing backend/role guarantees remain covered by the mandatory isolation suite.

Read-only PDF skill verification: downloaded maintenance.pdf has two pages and two embedded photos; extracted text contains both performers, cost, private note and void reason. Both rendered pages inspected: readable timestamped synthetic images, intact text/correction history, clean spacing and page numbering. This proves browser media/export paths using a simulated camera, not real-phone camera/keyboard/background/offline/push. Passing sample exports remain in ignored tmp/browser-service for QA only. Initial harness issues were successful empty RPC responses, simulated-video first-frame timing and checkbox automation; corrected the harness, not app logic. No product code or migration changed this slice, so no redundant bundle or Android rebuild. All 139 isolation checks passed after final test changes; overall latest unchanged full suite remains 201.

Cleanup complete: exact synthetic Storage paths deleted before transactional SQL, then the listed Auth accounts removed. Final query verified zero service-test tenants/users across runs and zero photos for the final run. Two submitted service records/photos in the passing run were disposable; original owner browser, real records and archived QA evidence untouched. Test credentials stayed in memory; failure diagnostics redact them. Source/test/status checkpoint to be committed and pushed.

Next useful acceptance: company-wide shared versus per-technician task completion through separate real UI sessions, and a short simulated browser offline/reconnect queue flow if feasible. Do not repeat verified invitation, service/photo/cost/void/export or build-6 checks without relevant changes. Actual native acceptance, verified sender/authorized email recipients, legacy material and offsite/hosted recovery targets remain external inputs. Keep Stage B/full visual redesign deferred. Small header fix from 04cc0b5 is verified locally but not in build 6; include it in the next meaningful native update rather than rebuild for testing-only changes.
