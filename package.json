{
  "name": "klever-assets",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "mobile": "pnpm --filter @klever/mobile start",
    "typecheck": "pnpm --filter @klever/mobile typecheck",
    "test": "vitest run",
    "test:isolation": "vitest run tests/database.test.ts",
    "check": "pnpm test && pnpm typecheck"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@pdf-lib/fontkit": "1.1.1",
    "pdf-lib": "1.17.1",
    "typescript": "6.0.3",
    "vitest": "5.0.0"
  }
}
